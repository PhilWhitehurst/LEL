<?php

/**
 * Fired when the plugin is uninstalled.
 *
 * @since      1.0.0
 * @package    lel-2017
 */
// If uninstall not called from WordPress uninstall, then exit.
if (!defined('WP_UNINSTALL_PLUGIN')) {
    exit;
}
